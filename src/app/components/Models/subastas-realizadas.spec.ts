import { SubastasRealizadas } from './subastas-realizadas';

describe('SubastasRealizadas', () => {
  it('should create an instance', () => {
    expect(new SubastasRealizadas()).toBeTruthy();
  });
});
